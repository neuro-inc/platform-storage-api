log:
  fields:
    service: storage
storage:
  cache:
    blobdescriptor: inmemory
  filesystem:
    rootdirectory: /var/lib/storage
http:
  addr: :5002
  headers:
    X-Content-Type-Options: [nosniff]
health:
  storagedriver:
    enabled: true
    interval: 10s
    threshold: 3
auth:
  token:
    realm: "http://localhost:5001/auth"
    service: "upstream"
    issuer: "Test Auth Server"
    rootcertbundle: "/etc/docker/storage/server.pem"
