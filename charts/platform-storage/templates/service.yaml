apiVersion: v1
kind: Service
metadata:
  name: {{ include "platformStorage.fullname" . }}
  labels: {{ include "platformStorage.labels.standard" . | nindent 4 }}
    service: platform-storage
  annotations:
    traefik.ingress.kubernetes.io/affinity: "true"
    traefik.ingress.kubernetes.io/session-cookie-name: NEURO_STORAGEAPI_SESSION
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: tcp-web
  selector:
    app: {{ include "platformStorage.name" . }}
    release: {{ .Release.Name }}
    service: platform-storage
